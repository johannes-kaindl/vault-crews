# Storage costs

Storage went up again this month. Most of it is the export archive, which the
old scheduler wrote twice — once as the working copy and once as the backup
nobody reads.

Open: whether the new scheduler still does that, or whether we can drop the
second copy once the migration is done.
