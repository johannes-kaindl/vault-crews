{
  "name": "vault-crews",
  "version": "0.1.0",
  "description": "Run autonomous local LLM agent teams (crews) on your vault via LM Studio",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit && node esbuild.config.mjs --production",
    "typecheck": "tsc --noEmit",
    "typecheck:test": "tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "lint": "eslint src",
    "lint:obsidian": "eslint src",
    "lint:portal": "eslint src --config eslint.portal.config.mjs",
    "deploy": "npm run build && cp main.js manifest.json styles.css \"${OBSIDIAN_PLUGIN_DIR:?set OBSIDIAN_PLUGIN_DIR}\"/",
    "version-bump": "node scripts/version-bump.mjs",
    "release": "node scripts/release.mjs",
    "check:pure": "sh -c \"! grep -rl \\\"from 'obsidian'\\\" src/core src/vendor src/obsidian/panel-view-model.ts 2>/dev/null\"",
    "check:bundle": "node esbuild.config.mjs --production && node scripts/check-bundle.mjs",
    "gate": "npm run lint && npm run typecheck && npm run typecheck:test && npm test && npm run check:pure && npm run check:bundle"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin"
  ],
  "author": "Johannes Kaindl",
  "license": "AGPL-3.0-or-later",
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20",
    "esbuild": "^0.23",
    "eslint": "^9",
    "eslint-plugin-obsidianmd": "latest",
    "js-yaml": "^4.1.0",
    "obsidian": "latest",
    "typescript": "^5.5",
    "typescript-eslint": "^8",
    "vitest": "^2"
  }
}